// Quicksort
// Try this 6-3

class Quicksort{

    // set up a call to the actuall Quicksort method.
    static void qsort(char items[]){
        qs(items, 0, items.length - 1);
    }

    // recurscive quicksort for characters
    private static void qs(char items[]. int left, int right){
        int i, j;
        char x, y;

    }
}
